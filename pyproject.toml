[project]
name = "snapcap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pillow>=12.2.0",
    "pywin32>=300 ; sys_platform == 'win32'",
]
